# [success] 2026-07-23

## Task
Create util/string-case.js exporting toKebab(s) converting camelCase to kebab-case. Follow ALL existing util-file conventions of this project. Verify with node.

Result: Done.

**Changed:** created `util/string-case.js` exporting `toKebab(s)`, which converts camelCase to kebab-case.

**Conventions followed** (matching existing `util/date-format.js`): file starts with the `// sdai-util-v2` marker and uses `module.exports = { ... }`.

**Verification** — `node`:
- `toK
