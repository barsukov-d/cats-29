# [success] 2026-07-22

## Task
Сделай страницу-визитку: файл card.html с именем Gena, строкой роли Developer и ссылкой на github.com/self-developer-ai. Простой чистый HTML без фреймворков.

---

# Approved contract (v1)

## Goal
Create a single self-contained business-card page in card.html displaying the name "Gena", the role line "Developer", and a link to github.com/self-developer-ai, using plain HTML with no frameworks.

## Boundaries
- IN: a single new file named card.html at the repository root
- IN: displays 

Result: Done. Created `card.html` at the repository root.

**What I built:** A single self-contained business-card page — plain HTML5, no frameworks, no external assets. It displays:
- The name **Gena** (`<h1>`)
- The role line **Developer** (`<p>`, distinct from the name)
- A clickable link `<a href="https
