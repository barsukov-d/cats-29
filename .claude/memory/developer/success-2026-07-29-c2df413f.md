# [success] 2026-07-29

Сделай одностраничный сайт-визитку цветочного магазина «Пион»: шапка с названием, секция «О нас», каталог из 5-7 букетов с названиями и ценами в рублях, секция доставки, контакты в футере. Без JavaScript и форм, весь текст на русском.

## Previous result
All checks pass. The line 5 match is a false positive — the regex `on[a-z]+=` caught the word "c**ontent**=" in the meta tag, not an event handler. No real `<script>`, `<form>`, `<input>`, `<textarea>`, submit buttons, or `on*=` handlers exist.


Result: Готово. Изменил цену первого букета в каталоге.

**Изменённый файл:**
- `index.html` — карточка «Нежность»: цена `3 200 ₽` → `2 500 ₽` (строка 141). Больше ничего не тронуто.

**Проверка:** в проекте нет сборки/тестов (только `README.md`), поэтому запускать нечего. Правка точечная — заменена ровно о
