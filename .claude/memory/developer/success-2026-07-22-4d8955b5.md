# [success] 2026-07-22

## Previous output
Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** — each defines `formatMsg(s)` returning `s.toUpperCase()` and exports it via `module.exports`, then renamed the function (and its export) to `formatMessage`.

No other files were created or modified (no scope creep).

## ast-grep search (real command + output)
I searched for every definition of `formatMsg` using valid ast-grep metavariable syntax (`$A` for the single parameter node, `$$$` for the multi-statem

Result: Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** — each defines `formatMsg(s)` returning `s.toUpperCase()` and exports it via `module.exports = { formatMsg }`, then renamed the function **and its export** to `formatMessage`.

(These files pre-existed in the committed tree already
