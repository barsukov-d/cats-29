# [success] 2026-07-22

## Previous output
Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** (created) — each defines and exports `formatMsg(s)` returning `s.toUpperCase()`, then renamed the function to `formatMessage` in all three.

## ast-grep search
Command run:
```
sg --pattern 'function formatMsg(s) { $$$ }' --lang js util-a.js util-b.js util-c.js
```
Output (all three definitions found):
```
util-a.js:1:function formatMsg(s) {
util-a.js:2:  return s.toUpperCase();
util-a.js:3:}
util-c.js:1:func

Result: Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** — each defines `formatMsg(s)` returning `s.toUpperCase()` and exports it via `module.exports`, then renamed the function (and its export) to `formatMessage`.

No other files were created or modified (no scope creep).

## ast-grep s
