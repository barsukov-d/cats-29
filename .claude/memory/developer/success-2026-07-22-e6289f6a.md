# [success] 2026-07-22

## Task
Create files util-a.js, util-b.js and util-c.js, each defining and exporting a function formatMsg(s) returning s.toUpperCase(). Then use ast-grep (the sg CLI) to find every definition of formatMsg across the files and include the sg command you ran and its output in your report. Finally rename the function to formatMessage in all three files using sg or manual edits, verifying with node -e that require("./util-a.js").formatMessage("hi") returns "HI".

Result: Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** (created) — each defines `formatMsg(s)` returning `s.toUpperCase()` and exports it via `module.exports = { formatMsg }`, then renamed the function **and its export** to `formatMessage` in all three.

## ast-grep search
Command run:
