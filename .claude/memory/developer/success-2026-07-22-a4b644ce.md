# [success] 2026-07-22

## Previous output
Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** (created) — each defines `formatMsg(s)` returning `s.toUpperCase()` and exports it via `module.exports = { formatMsg }`, then renamed the function **and its export** to `formatMessage` in all three.

## ast-grep search
Command run:
```
sg --pattern 'function formatMsg(s) { $$$BODY }' --lang js util-a.js util-b.js util-c.js
```
Output (all three definitions found):
```
util-a.js:1:function formatMsg(s) {
util-

Result: Done.

## What I changed
- **`util-a.js`, `util-b.js`, `util-c.js`** — each defines `formatMessage(s)` returning `s.toUpperCase()` and exports it via `module.exports = { formatMessage }`.

The files started in a leftover `formatMessage` state from a prior attempt, so I first rewrote all three back t
