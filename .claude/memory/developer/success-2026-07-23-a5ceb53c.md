# [success] 2026-07-23

## Task
Create util/date-format.js exporting formatDate(d) returning YYYY-MM-DD. Project convention you must honor and remember: all util files must start with the comment line // sdai-util-v2 and export via module.exports. Verify with node.

Result: Done.

**What I changed**
- Created `util/date-format.js` — exports `formatDate(d)` returning a `YYYY-MM-DD` string with month and day zero-padded. Follows the project convention: first line `// sdai-util-v2` and export via `module.exports`.

**Verification**
- Ran `node -e` requiring the module:
