# [success] 2026-07-20

Create a minimal index.html containing the text Hello E2E. Single static file, nothing else.

Result: `index.html` already exists at the repo root and already satisfies the request — a single static HTML file with no build tooling, containing the text **Hello E2E** in its body:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>E2E</title>
</head>
<body>
    Hello
